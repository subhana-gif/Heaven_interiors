// controllers/orderController.js

// Render the Orders Page (empty for now)
exports.renderOrdersPage = (req, res) => {
    res.render('admin/orders');  // Correct path to 'orders.ejs' inside the 'admin' folder
};
