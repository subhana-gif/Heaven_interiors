<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body, html {
      height: 100%;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
    }

    .content-wrapper {
      flex: 1;
      display: flex;
    }

    .content {
      margin-left: 250px; /* Same as sidebar width */
      padding: 2em;
      flex-grow: 1;
    }

    .navbar {
      height: 3.75em;
      background-color: #f8f9fa;
      border-bottom: 1px solid #dee2e6;
    }

    .logo {
      position: absolute;
      top: 0.5em;
      left: 1.25em;
      font-size: 1.225em; 
      font-weight: bold;
      color: #2d4739;
      text-align: center;
    }

    .logo .interior {
      font-size: 0.875em;
      font-weight: 300;
      display: block;
      color: #2d4739;
      letter-spacing: 0.009375em;
      margin-top: -0.625em;
    }

    .btn-search {
      margin-left: auto;
    }

    .sidebar {
      height: calc(100vh - 3.75em); 
      width: 250px;
      position: fixed;
      top: 3.75em;
      left: 0;
      background-color: #e3e2e2; 
      color: #000;
      padding: 1em;
    }

    .sidebar ul {
      list-style: none;
      padding: 0;
    }

    .sidebar ul li {
      margin: 0.5em 0;
    }

    .sidebar ul li a {
      text-decoration: none;
      color: #000;
      display: block;
      padding: 0.5em;
      border-radius: 0.25em;
      background-color: #f8f9fa;
    }

    .sidebar ul li a:hover,
    .sidebar ul li a.active {
      background-color: #2d4739; 
      color: #fff;
    }

    .footer {
      background-color: #f8f9fa;
      color: #6c757d;
      padding: 1em 0;
      text-align: center;
      width: 100%;
    }

    .footer p {
      margin: 0;
    }
  </style>
  <title>Admin Panel</title>
</head>
<body>

  <%- include('partials/navbar_sidebar', { body: body }) %>

  <div class="content-wrapper">
    <div class="content">
      <!-- Load the dynamic content here -->
      <%- include(body) %>
    </div>
  </div>

  <footer class="footer">
    <div class="container">
      <p>&copy; 2024 Heaven Interior. All rights reserved.</p>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
